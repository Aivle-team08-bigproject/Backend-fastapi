# 자동화 파이프라인 발표 스크립트

## 발표자를 위한 안내

이 문서는 자동화 파이프라인을 처음 설명하는 발표자를 위한 대본이다. 문장을 모두 외울 필요는
없다. 각 장의 **핵심 한 문장**만 기억하고, 나머지는 화면을 보면서 천천히 설명하면 된다.

권장 발표 시간:

- 짧은 발표: 7~10분
- 기본 발표: 12~15분
- 시연 및 질의응답 포함: 20분

함께 공유할 기술 문서:

- `AUTOMATION_PIPELINE_ARCHITECTURE.md`

발표에서 가장 중요한 태도는 “모든 것이 완성됐다”고 말하는 것이 아니라, 다음 세 가지를
명확하게 구분하는 것이다.

1. 어떤 문제를 해결하려고 했는가
2. 지금 실제로 구현하고 검증한 것은 무엇인가
3. 운영 단계로 가기 위해 무엇이 더 필요한가

---

# 1장. 발표 시작

## 화면에 보여줄 내용

```text
Supervisor·Worker 기반 데이터 자동화 파이프라인

자연어 요구사항
→ 데이터 조건 설계
→ 실제 데이터 선별
→ 안전한 데이터 가공
```

## 핵심 한 문장

> 사용자의 자연어 요청을 실제 데이터 선별과 가공까지 단계별로 연결한 자동화 구조입니다.

## 발표 대본

“안녕하세요. 오늘은 저희가 구현한 Supervisor·Worker 기반 데이터 자동화 파이프라인을
설명드리겠습니다.

이 시스템의 목표는 사용자가 자연어로 원하는 데이터를 요청하면, 그 요구사항을 분석하고,
실제 데이터에서 조건에 맞는 행을 선별한 뒤, 안전하게 가공해서 CSV나 API, 보고서 형태로
제공하는 것입니다.

특히 한 프로그램이 모든 일을 한 번에 처리하는 방식이 아니라, 각 단계를 독립된 Worker로
나누고 단계별 결과를 데이터베이스에 저장하도록 설계했습니다.”

---

# 2장. 왜 이 구조가 필요한가

## 화면에 보여줄 내용

```text
기존 방식의 문제

한 프로세스가 모든 단계 실행
→ 중간 결과 확인 어려움
→ 실패 지점 추적 어려움
→ 특정 단계만 재실행하기 어려움
→ 사람의 승인 시점이 불명확함
```

## 핵심 한 문장

> 긴 작업을 작은 단계로 나누면 중간 결과를 확인하고 실패한 위치를 추적하기 쉬워집니다.

## 발표 대본

“먼저 이 구조가 필요한 이유를 말씀드리겠습니다.

요구사항 분석, 데이터 선별, 데이터 조회, 데이터 가공을 한 프로세스에서 모두 실행하면
중간에 어떤 결과가 만들어졌는지 확인하기 어렵습니다.

예를 들어 요구사항을 잘못 해석했는지, 필터가 잘못 만들어졌는지, 실제 가공 과정에서 문제가
생겼는지 구분하기 어렵습니다.

그래서 전체 작업을 작은 단계로 나눴습니다. 각 단계가 자기 일만 수행하고 결과를 DB에 저장한
뒤 종료하게 만들었습니다. 이렇게 하면 어느 단계에서 어떤 입력과 출력이 있었는지 확인할 수
있고, Client가 중간 결과를 검토한 뒤 다음 단계로 진행할 수 있습니다.”

---

# 3장. 전체 구성 요소

## 화면에 보여줄 내용

기술 문서 `2. 전체 개념`의 Mermaid 그림을 보여준다.

## 핵심 한 문장

> FastAPI는 Redis에 작업을 등록하고, Worker Consumer는 Supervisor Worker와 단계 Worker를 실행하며, PostgreSQL은 상태를 기억합니다.

## 발표 대본

“전체 시스템에는 다섯 가지 핵심 구성 요소가 있습니다.

첫 번째는 FastAPI입니다. 사용자가 Job을 만들거나 결과를 승인하고 상태를 조회하는 API
진입점이며, Supervisor를 직접 실행하지 않고 Redis Queue에 실행 작업을 등록합니다.

두 번째는 Redis입니다. Redis는 실행할 Worker 작업을 Queue에 보관하고 Worker Consumer에
전달합니다.

세 번째는 Worker Consumer입니다. Worker Consumer는 Redis에서 작업을 가져와 작업 이름에 맞는
Worker 함수를 실행합니다.

네 번째는 Supervisor Worker와 단계별 Worker입니다. Supervisor도 Worker의 한 종류로 다음
단계를 결정합니다. 단계별 Worker는 요구사항 분석, 데이터 선별, 조회, 가공 중 하나만 실행합니다.

마지막은 PostgreSQL입니다. Job 상태와 각 Worker의 입력, 출력, 검증 결과, 실행 시간을
저장합니다.

쉽게 비유하면 FastAPI는 접수 창구, Redis는 업무 전달함, Worker Consumer는 전달함에서 업무를
꺼내 실행하는 담당자, Supervisor Worker는 업무 배정자, 단계별 Worker는 실제 업무 담당자,
PostgreSQL은 업무 기록장입니다.”

---

# 4장. Supervisor Worker와 단계별 Worker의 차이

## 화면에 보여줄 내용

| 구분 | Supervisor Worker | 단계별 Worker |
|---|---|---|
| 역할 | 다음 단계 결정 | 실제 단계 실행 |
| 실행 시간 | 짧음 | 작업이 끝날 때까지 |
| 데이터 처리 | 하지 않음 | 수행 |
| DB 작업 | Worker 생성, 상태 변경 | 결과 및 검증 저장 |
| 종료 시점 | Worker 생성 후 | 자기 단계 완료 후 |

## 핵심 한 문장

> Supervisor Worker는 일을 배정하고, 단계별 Worker는 배정된 일 하나를 처리합니다.

## 발표 대본

“Supervisor Worker와 단계별 Worker의 역할 차이가 이 구조에서 중요합니다.

Supervisor도 Redis를 통해 실행되는 Worker입니다. 다만 실제 분석이나 가공 대신 DB를 보고
다음에 필요한 단계 Worker 하나를 생성하고 Redis에 실행 작업을 등록합니다.

반대로 Worker는 실제 업무를 수행합니다. Worker가 실행되면 자신의 상태를 `RUNNING`으로
바꾸고 Agent 또는 Python 처리기를 호출합니다. 작업이 끝나면 결과를 DB에 저장하고
승인 대기 상태로 바꾼 뒤 종료합니다.

따라서 Supervisor Worker는 업무 배정 담당자이고 단계별 Worker는 실제 업무 담당자라고
이해하면 됩니다.”

---

# 5장. 전체 단계

## 화면에 보여줄 내용

```text
1. REQUIREMENT_ANALYSIS
2. DATA_SELECTION
3. DATA_RETRIEVAL
4. DATA_PROCESSING
5. 최종 승인
```

## 핵심 한 문장

> 분석, 설계, 실제 선별, 가공을 서로 다른 단계로 분리했습니다.

## 발표 대본

“파이프라인은 크게 네 개의 실행 단계로 나뉩니다.

첫 번째는 요구사항 분석입니다. 사용자의 자연어 요청을 구조화합니다.

두 번째는 데이터 선별입니다. 어떤 테이블과 필터, 컬럼이 필요한지 설계합니다.

세 번째는 데이터 조회입니다. 앞 단계에서 설계한 조건을 실제 CSV에 적용해 필요한 행만
선별합니다.

네 번째는 데이터 가공입니다. 선별된 데이터만 읽어서 가명화, 결측값 처리, 중복 제거를 하고
CSV, API, 시각화, 보고서 산출물을 만듭니다.

각 단계가 완료되면 결과를 저장하고 Client 승인을 기다립니다.”

---

# 6장. 요구사항 분석 Agent

## 화면에 보여줄 내용

```text
입력
“일본에서 숙박 결제를 한 20대 여성의 데이터를 CSV로 제공해줘.”

출력
국가: 일본
업종: 숙박
연령대: 20대
성별: 여성
출력 형식: CSV
```

## 핵심 한 문장

> 요구사항 분석 Agent는 자연어를 다음 단계가 이해할 수 있는 JSON으로 바꿉니다.

## 발표 대본

“요구사항 분석 Agent는 사용자의 자연어를 읽고 구조화된 JSON으로 바꿉니다.

예를 들어 사용자가 일본, 숙박, 20대 여성 데이터를 요청하면 국가, 업종, 연령대, 성별이라는
조건으로 분리합니다. 그리고 결과를 CSV로 원하는지도 함께 추출합니다.

이 단계에서는 실제 데이터를 읽지 않습니다. 사용자가 무엇을 원하는지 해석하는 역할만 합니다.

현재 이 Agent는 Strands Agents SDK를 사용하고 DeepSeek API를 호출합니다. 모델이 JSON 형식을
지키지 못하거나 일시적으로 호출에 실패할 경우 최대 세 번까지 재시도합니다.”

---

# 7장. 데이터 선별 Agent

## 화면에 보여줄 내용

```text
요구사항 분석 결과
→ 필요한 테이블 결정
→ 필터 조건 설계
→ 필요한 컬럼 설계
```

## 핵심 한 문장

> 데이터 선별 Agent는 실제 행을 고르는 Agent가 아니라, 어떤 데이터를 골라야 하는지 설계하는 Agent입니다.

## 발표 대본

“다음은 데이터 선별 Agent입니다.

이 부분에서 혼동하기 쉬운 점이 있습니다. 데이터 선별 Agent가 CSV 전체를 읽고 직접 행을
고르는 것은 아닙니다.

이 Agent는 어떤 테이블이 필요한지, 어떤 필터를 적용해야 하는지, 어떤 컬럼이 필요한지
조회 계획을 만듭니다.

LLM에게 원본 데이터 전체를 보내지 않는 이유는 개인정보 보호, 비용, 결과의 결정성을
고려했기 때문입니다.

즉, Agent는 조건을 설계하고 실제 조건 실행은 다음 Python Worker가 담당합니다.”

---

# 8장. DATA_RETRIEVAL Worker

## 화면에 보여줄 내용

```text
식별 조건
국가 = 일본
업종 = 숙박
성별 = 여성

          ↓ 실제 CSV에 적용

destination_country_name = 일본
spend_category = lodging
gender = F
```

## 핵심 한 문장

> DATA_RETRIEVAL Worker가 Agent의 논리 조건을 실제 CSV 컬럼과 값으로 바꾸고 행을 선별합니다.

## 발표 대본

“데이터 조회 Worker는 이번 구현에서 매우 중요한 연결 부분입니다.

이전에는 데이터 선별 Agent가 조건을 만들어도 그 조건이 실제 CSV에 적용되지 않아 1,600행
전체가 가공 단계로 전달됐습니다.

현재는 국가라는 조건을 `destination_country_name` 컬럼으로, 여성이라는 값을 `F`로,
숙박이라는 값을 `lodging` 또는 `호텔/숙박`으로 변환합니다.

그다음 모든 조건을 실제 CSV에 적용하고, 조건에 맞는 행만 별도의 `selected.csv`로 저장합니다.

매핑할 수 없는 조건이 있거나 결과가 0행이면 전체 데이터를 그냥 넘기지 않고 실패 처리합니다.
이 안전장치가 없으면 요구사항과 다른 데이터가 가공될 수 있기 때문입니다.”

---

# 9장. 데이터 가공 Worker

## 화면에 보여줄 내용

```text
selected.csv
→ 체크섬 확인
→ 정규화
→ 중복 제거
→ 결측값 처리
→ 직접 식별자 가명화
→ CSV·API·시각화·보고서 생성
```

## 핵심 한 문장

> 데이터 가공 Worker는 선별된 데이터만 결정론적 Python 규칙으로 처리합니다.

## 발표 대본

“데이터 가공 Worker는 선별된 CSV만 읽습니다.

먼저 조회 단계에서 기록한 SHA-256 체크섬과 현재 파일이 같은지 확인합니다. 중간에 파일이
변경됐다면 가공을 중단합니다.

그다음 공백과 null 값을 정리하고, 완전히 같은 행은 중복으로 제거합니다. 필요한 경우 결측값을
중앙값이나 최빈값으로 보정합니다.

`customer_id`, 이메일, 전화번호 같은 직접 식별자는 HMAC-SHA256 방식으로 가명화합니다.

중요한 점은 이 가공 과정이 LLM이 아니라 Python 규칙으로 실행된다는 것입니다. 따라서 같은
입력에는 같은 결과가 나오며, 원본 데이터 행을 LLM에 보내지 않습니다.”

---

# 10장. 실제 CSV 검증 결과

## 화면에 보여줄 내용

```text
실제 CSV 원본           1,600행

조건
- 국가: 일본
- 업종: 숙박
- 성별: 여성

선별 결과                  40행
가공 입력                  40행
가공 출력                  40행
가명화                     40건
```

## 핵심 한 문장

> 식별 조건이 실제 데이터에 적용되어 1,600행 중 40행만 가공되는 것을 확인했습니다.

## 발표 대본

“실제 테스트에서는 해외 여행 결제 CSV 1,600행을 사용했습니다.

일본, 숙박, 여성이라는 세 조건을 적용한 결과 40행이 선별됐습니다.

그리고 데이터 가공 Worker의 입력도 40행, 출력도 40행이었습니다. 따라서 이전처럼 1,600행
전체가 무조건 가공되는 것이 아니라, 식별 조건이 실제 결과에 영향을 주는 것을 확인했습니다.

40개 행의 `customer_id`도 모두 가명화됐고, 선별 파일 체크섬 검증과 가공 CSV 생성까지
정상적으로 완료됐습니다.”

---

# 11장. DB 상태 변화

## 화면에 보여줄 내용

```text
QUEUED
→ WORKER_CREATED
→ RUNNING
→ WAITING_HITL
→ 승인
→ 다음 WORKER_CREATED
→ ...
→ COMPLETED
```

## 핵심 한 문장

> 모든 단계의 현재 위치와 결과를 DB 상태로 확인할 수 있습니다.

## 발표 대본

“Job이 처음 만들어지면 `QUEUED` 상태입니다.

Supervisor가 Worker를 만들면 `WORKER_CREATED`, Worker가 실행되면 `RUNNING`이 됩니다.

Worker가 결과를 저장하면 `WAITING_HITL`, 즉 사람의 승인을 기다리는 상태가 됩니다.

중간 단계가 승인되면 Supervisor가 다시 호출되어 다음 Worker를 생성합니다. 마지막 데이터
가공 결과가 승인되면 `COMPLETED`가 됩니다.

이 상태들을 PostgreSQL에 저장하기 때문에 프로세스가 종료되더라도 현재 단계와 과거 실행
결과를 다시 확인할 수 있도록 설계했습니다.”

---

# 12장. 현재 구현 완료 범위

## 화면에 보여줄 내용

```text
완료
✓ 자연어 요구사항 분석
✓ 데이터 조건 설계
✓ 조건의 실제 CSV 적용
✓ 선별 CSV 생성
✓ 선별 데이터만 가공
✓ 체크섬 검증
✓ 직접 식별자 가명화
✓ Supervisor 승인 체인 단위 테스트
```

## 핵심 한 문장

> 개별 단계와 단계 사이의 데이터 계약은 실제 CSV로 검증했습니다.

## 발표 대본

“현재까지 구현하고 검증한 범위를 정리하겠습니다.

자연어 요구사항 분석, 데이터 조건 설계, 조건을 실제 CSV에 적용하는 기능, 선별 파일 생성,
선별 데이터 가공, 체크섬 검증과 가명화를 구현했습니다.

또한 Supervisor가 요구사항 분석부터 데이터 선별, 데이터 조회, 데이터 가공 순서로 Worker를
생성하는 승인 체인을 단위 테스트로 검증했습니다.

실제 데이터 테스트에서는 원본 1,600행에서 조건에 맞는 40행만 가공되는 것도 확인했습니다.”

---

# 13장. 아직 해결해야 할 문제

## 화면에 보여줄 내용

```text
다음 과제

1. 실제 PostgreSQL end-to-end 테스트
2. 전체 승인 체인 end-to-end 테스트
3. CSV 업로드 및 Artifact 관리
4. Queue와 DB 사이 장애 복구
5. 대용량 결과 외부 스토리지 저장
6. 동시 실행 및 중복 방지
7. 고급 필터 연산자
```

## 핵심 한 문장

> 다음 단계는 개별 기능 개발보다 실제 실행 환경에서 자동으로 이어지게 만드는 것입니다.

## 발표 대본

“아직 해결해야 할 부분도 명확히 남아 있습니다.

첫 번째로 Supervisor 상태 전이를 실제 PostgreSQL을 사용한 end-to-end 환경에서 검증해야 합니다.

두 번째로 Redis Queue와 Worker Consumer를 통한 자동 실행은 연결됐지만, 모든 승인 단계를
거쳐 최종 가공까지 완주하는 end-to-end 테스트가 더 필요합니다.

현재 Docker Compose는 FastAPI, PostgreSQL, Redis, Worker Consumer 네 서비스로 구성됩니다.
Supervisor는 별도 API가 아니라 Worker Consumer가 Redis 작업으로 실행하는 Worker의 한 종류입니다.

네 번째로 실제 Client가 서버 내부 파일 경로를 직접 전달하지 않도록 CSV 업로드와 Artifact
관리 구조가 필요합니다.

그 외에도 실패한 Worker 승인 차단, 대용량 결과 파일의 외부 저장, 동시 실행 잠금, 금액·날짜·
제외 조건과 같은 고급 필터가 필요합니다.

따라서 현재는 핵심 데이터 흐름이 검증된 단계이고, 다음 목표는 이를 운영 가능한 자동 실행
환경으로 만드는 것입니다.”

---

# 14장. 반려 처리에 대한 설명

## 화면에 보여줄 내용

```text
현재 발표 범위
단계 실행 → 결과 저장 → 승인 → 다음 단계

향후 범위
반려 원인 분류 → 재실행 또는 롤백 정책
```

## 핵심 한 문장

> 반려 이후 자동 롤백은 아직 확정된 범위가 아니며 향후 정책으로 분리했습니다.

## 발표 대본

“이번 발표에서는 정상 승인 흐름을 중심으로 설명했습니다.

Client가 결과를 반려했을 때 어떤 단계로 돌아갈지, 자동으로 다시 실행할지, 운영자가 개입할지는
아직 정책을 확정해야 하는 부분입니다.

따라서 현재 핵심 흐름은 단계 실행, 결과 저장, Client 승인, 다음 단계 생성입니다. 반려에 따른
자동 롤백은 향후 별도 정책으로 설계할 예정입니다.”

---

# 15장. 마무리

## 화면에 보여줄 내용

```text
Agent는 조건을 설계하고
Python Worker는 실제 데이터에 조건을 적용하며
Supervisor는 단계 실행을 통제하고
PostgreSQL은 전체 과정을 기록한다.
```

## 핵심 한 문장

> LLM의 판단과 실제 데이터 처리를 분리해 추적 가능하고 안전한 파이프라인을 만들었습니다.

## 발표 대본

“마지막으로 전체 구조를 한 문장으로 정리하겠습니다.

Agent는 사용자의 요구사항과 데이터 조건을 설계합니다. Python Worker는 그 조건을 실제
데이터에 결정적으로 적용합니다. Supervisor는 어떤 단계가 실행될지 통제하고, PostgreSQL은
전체 입력과 출력, 상태 변화를 기록합니다.

이를 통해 LLM이 원본 데이터를 직접 처리하지 않으면서도 자연어 요구사항을 실제 데이터
선별과 가공까지 연결할 수 있게 했습니다.

현재 핵심 데이터 흐름은 실제 CSV로 검증했으며, 다음 단계에서는 PostgreSQL 통합,
Worker 자동 실행, 파일 업로드와 운영 안정성을 완성할 예정입니다.

이상으로 발표를 마치겠습니다. 감사합니다.”

---

# 시연 순서

발표 중 시연을 한다면 긴 API 전체 흐름보다 이미 검증된 결과를 짧게 보여주는 것이 안전하다.

## 시연 1. 원본 CSV 확인

```text
원본 행 수: 1,600
컬럼 수: 17
```

원본 데이터 행 자체나 식별자 값은 화면에 노출하지 않는다.

## 시연 2. 식별 조건

```json
{
  "국가": "일본",
  "업종": "숙박",
  "성별": "여성"
}
```

## 시연 3. 필터 매핑

```text
국가 → destination_country_name → 일본
업종 → spend_category, mcc_name → lodging, 호텔/숙박
성별 → gender → F
```

## 시연 4. 결과

```text
input_rows       1,600
selected_rows       40
processing_input    40
processing_output   40
anonymized_rows     40
```

## 시연 시 주의사항

- `.env` 내용을 화면에 열지 않는다.
- API 키를 출력하지 않는다.
- 실제 `customer_id`를 출력하지 않는다.
- `--verbose`로 전체 원본 행이나 Base64 CSV를 출력하지 않는다.
- FastAPI가 Supervisor를 직접 호출한다고 설명하지 않는다.
- dry-run 테스트와 신규 Supervisor DB 흐름을 같은 테스트라고 설명하지 않는다.

---

# 예상 질문과 답변

## Q1. 왜 LLM이 CSV를 직접 읽게 하지 않았나요?

“원본 행을 LLM에 전달하면 개인정보 보호와 비용 문제가 생길 수 있고, 같은 입력에도 결과가
달라질 수 있습니다. 그래서 LLM은 조건 설계만 하고 실제 데이터 필터와 가공은 결정론적
Python 코드가 수행하도록 분리했습니다.”

## Q2. 데이터 선별 Agent와 DATA_RETRIEVAL Worker는 무엇이 다른가요?

“데이터 선별 Agent는 무엇을 골라야 하는지 계획합니다. DATA_RETRIEVAL Worker는 그 계획을
실제 CSV 컬럼과 값으로 변환해 행을 추출합니다.”

## Q3. 전체 1,600행을 아직도 가공하나요?

“아닙니다. 현재는 식별 조건을 실제 CSV에 적용합니다. 일본, 숙박, 여성 조건 테스트에서는
1,600행 중 40행만 선별됐고 가공 Worker도 40행만 처리했습니다.”

## Q4. 필터를 매핑할 수 없으면 어떻게 되나요?

“전체 데이터를 그대로 넘기지 않고 DATA_RETRIEVAL Worker를 실패 처리합니다. 잘못된 조건을
무시하고 성공 처리하면 요구사항과 다른 결과가 만들어질 수 있기 때문입니다.”

## Q5. 원본 데이터가 PostgreSQL에 저장되나요?

“원본 CSV 행은 Supervisor DB에 저장하지 않습니다. DB에는 파일 경로, 행 수, 컬럼, 적용한
필터와 SHA-256 체크섬 같은 메타데이터만 저장하도록 설계했습니다.”

## Q6. 조회 후 CSV가 변경되면 어떻게 하나요?

“조회 Worker가 SHA-256 체크섬을 저장합니다. 가공 Worker가 실행될 때 체크섬을 다시 계산하고,
값이 다르면 파일이 변경된 것으로 판단해 가공을 거부합니다.”

## Q7. Worker는 자동으로 실행되나요?

“FastAPI가 Redis Queue에 Supervisor Worker 작업을 등록하고, Worker Consumer가 이를 실행합니다.
Supervisor Worker도 다음 단계 Worker를 Redis에 등록하므로 단계 Worker는 자동 실행됩니다.”

## Q8. PostgreSQL까지 실제 통합 테스트했나요?

“Supervisor 상태 전이는 단위 테스트로 검증했고, 데이터 조회와 가공은 실제 CSV로 검증했습니다.
실제 PostgreSQL 컨테이너를 사용한 전체 end-to-end 상태 전이 테스트는 다음 작업 범위입니다.”

## Q9. 반려되면 자동으로 이전 단계로 돌아가나요?

“반려 이후 자동 재실행과 롤백은 아직 정책이 확정되지 않았습니다. 현재 발표 범위는 정상 승인
흐름이며, 반려 사유 저장과 후속 처리 정책은 다음 단계에서 분리해 설계할 예정입니다.”

## Q10. 이 구조의 가장 큰 장점은 무엇인가요?

“단계별 입력과 출력을 추적할 수 있고, LLM과 원본 데이터 처리를 분리했으며, 중간 승인 지점을
둘 수 있다는 점입니다. 문제가 발생했을 때 어느 단계에서 실패했는지도 확인하기 쉽습니다.”

## Q11. 운영 환경에서는 CSV를 어떻게 전달할 예정인가요?

“현재 로컬 테스트에서는 공유 경로를 사용합니다. 운영에서는 Client가 CSV를 업로드하면
Artifact ID를 발급하고, 실제 파일은 S3 같은 객체 스토리지에 저장하는 방식으로 변경할
계획입니다.”

## Q12. 같은 Worker가 두 번 실행될 위험은 없나요?

“현재 단일 흐름에서는 활성 Worker를 검사하지만, 여러 Worker Consumer가 동시에 실행되는 운영
환경에서는 DB 잠금과 idempotency가 추가로 필요합니다. `FOR UPDATE SKIP LOCKED` 방식과
Job당 활성 Worker 하나 제약을 적용할 예정입니다.”

---

# 발표 전 체크리스트

## 문서

- [ ] `AUTOMATION_PIPELINE_ARCHITECTURE.md` 공유
- [ ] 이 발표 스크립트 준비
- [ ] Mermaid 그림이 발표 도구에서 정상 표시되는지 확인

## 시연

- [ ] Docker Desktop 실행 여부 확인
- [ ] `backend2-agent-test` 이미지 확인
- [ ] 테스트 CSV 경로 확인
- [ ] `.env` 파일 존재 여부 확인
- [ ] API 키가 화면에 표시되지 않도록 터미널 정리
- [ ] 전체 원본 데이터 대신 행 수와 결과 요약만 출력

## 발표 내용

- [ ] Supervisor와 Worker 차이를 먼저 설명
- [ ] 데이터 선별 Agent와 실제 조회 Worker 차이를 설명
- [ ] `1,600 → 40 → 40` 결과 강조
- [ ] 실제 구현과 향후 구현을 구분
- [ ] 반려 자동 롤백은 향후 범위라고 설명
- [ ] Redis 자동 dispatch는 완료됐고 전체 승인 체인 완주 테스트가 남았다고 설명

---

# 발표 직전 외울 다섯 문장

발표가 긴장된다면 다음 다섯 문장만 기억해도 전체 설명을 이어갈 수 있다.

1. “Supervisor는 일을 직접 하지 않고 다음 Worker 하나를 생성합니다.”
2. “Agent는 조건을 설계하고 Python Worker는 그 조건을 실제 데이터에 적용합니다.”
3. “원본 데이터 행은 LLM에 전달하지 않습니다.”
4. “실제 CSV 1,600행에서 조건에 맞는 40행만 선별하고 가공했습니다.”
5. “다음 목표는 전체 승인 체인 완주와 Queue·DB 장애 복구입니다.”
